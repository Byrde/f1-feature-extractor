# Project Backlog


| Epic                       | Task Description                           | Acceptance Criteria                                                                                                                                                                                                                        | Status          | Prototype | Notes                                                                       |
| -------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------- | --------- | --------------------------------------------------------------------------- |
| **Session Data Ingestion** | Define domain models for raw session data  | Dataclasses exist for `Session`, `Driver`, `Lap`, `Stint`, `PitStop`, `WeatherSnapshot` with all fields from OpenF1 API mapped                                                                                                             | `Complete`      |           | Foundation for all other work                                               |
| **Session Data Ingestion** | Implement OpenF1 API client                | Client can query any endpoint with filters; returns typed responses                                                                                                                                                                        | `Complete`      |           | Native fetch; batch use only                                                |
| **Session Data Ingestion** | Implement session data fetcher             | Given a `session_key`, fetches all relevant endpoints (laps, stints, pit, car_data, weather, drivers, sessions) and assembles a populated `Session` aggregate                                                                              | `Complete`      |           | Orchestrates API client; depends on domain models and API client            |
| **Stint Classification**   | Implement stint classifier                 | Each stint is assigned a `stint_type`: `quali_sim`, `long_run`, `aero_check`, or `installation`. Quali sim: fresh soft + ≤3 push laps + fast pace. Long run: 5+ timed laps. Installation: first stint, 1 lap, slow. Aero check: remainder. | `Complete`      |           | Pit-out laps excluded from push lap count; outliers (>107% median) excluded |
| **Pace Analysis**          | Compute best lap time per compound         | For each driver, returns the fastest `lap_duration` for each compound used, excluding pit out laps                                                                                                                                         | `Complete`      |           | Grouped by compound from stint data                                         |
| **Pace Analysis**          | Compute long-run average pace              | For each driver, returns mean `lap_duration` across all laps in `long_run` stints, excluding pit out laps and outliers (>107% of stint mean)                                                                                               | `Complete`      |           | Depends on stint classification                                             |
| **Pace Analysis**          | Compute sector performance                 | For each driver, returns best and mean duration for each sector (S1, S2, S3) from timed laps                                                                                                                                               | `Complete`      |           |                                                                             |
| **Degradation Analysis**   | Compute tyre degradation rate              | For each long-run stint, compute the linear regression slope of `lap_duration` over lap sequence. Result in seconds/lap.                                                                                                                   | `Complete`      |           | Only on long-run stints; exclude outliers                                   |
| **Degradation Analysis**   | Compute fuel-corrected long-run pace       | Apply ~0.06s/lap fuel correction to long-run lap times (subtract `0.06 * lap_index_in_stint`), then recompute average                                                                                                                      | `Complete`      |           | Approximate; document the assumption                                        |
| **Degradation Analysis**   | Compute consistency metric                 | Standard deviation of lap times within long-run stints per driver                                                                                                                                                                          | `Complete`      |           | Lower = more consistent                                                     |
| **Speed Analysis**         | Extract speed trap and intermediate speeds | For each driver, capture best and mean `st_speed`, `i1_speed`, `i2_speed` from timed laps                                                                                                                                                  | `Complete`      |           |                                                                             |
| **Speed Analysis**         | Compare speeds across stint types          | For each driver, compare mean speed trap in quali-sim stints vs. long-run stints                                                                                                                                                           | `Complete`      |           | Delta indicates fuel-load effect on straight-line speed                     |
| **Session Context**        | Capture weather and session metadata       | `WeatherSnapshot` values (track temp, air temp, humidity, rainfall, wind) captured for the session. Session metadata (circuit, session type, date) stored.                                                                                 | `Complete`      |           | Weather updates every minute; store range/mean                              |
| **Feature Assembly**       | Define DriverFeatures model                | Dataclass with all KPI fields: pace metrics, degradation metrics, speed metrics, consistency, stint summaries, session context                                                                                                             | `Complete`      |           | The output contract                                                         |
| **Feature Assembly**       | Implement feature assembler                | Given a populated `Session` aggregate, produces a `DriverFeatures` for each driver by orchestrating all analysis functions                                                                                                                 | `Complete`      |           | Depends on all analysis tasks                                               |
| **Feature Assembly**       | Implement session-level ranking            | Rank drivers within the session by long-run pace, best lap, degradation rate, and consistency                                                                                                                                              | `Complete`      |           | Relative positioning aids GridRival decisions                               |


## Column Guide

- **Status**: Must follow `TODO` → `In Progress` → `Ready to Test` → `Complete`
- **Prototype**: Maintained by prototyping workflow. Leave empty for new tasks. When prototype exists: `prototypes/[slug].[ext]` + brief findings.
- **Notes**: Include testability considerations for non-trivial verification:
  - If feature involves long-running processes, note start/verify/stop pattern needed
  - If feature requires user interaction for testing, note the handoff scenario
  - If verification is straightforward, no note needed

